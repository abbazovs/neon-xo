{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
        "startCommand": "node backend/dist/db/migrate.js && echo PRE_INDEX && node backend/dist/index.js 2>&1; echo EXIT_CODE:$?",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
