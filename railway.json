{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo \"[boot] PORT=$PORT NODE_ENV=$NODE_ENV\" && node backend/dist/db/migrate.js && echo \"[boot] migrate exit=$?, starting server\" && node backend/dist/index.js; echo \"[boot] index.js exited code=$?\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
